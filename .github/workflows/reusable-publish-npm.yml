---
name: Reusable npm Publishing Workflow (deprecated)

# DEPRECATED (#965): single-package publishing is the one-element case of a
# package set. This wrapper forwards to reusable-publish-npm-set.yml (the
# audited, idempotent, verified publish loop) using its single-directory
# shape (order: "."). It will be removed in a future release — switch to
# reusable-publish-npm-set.yml directly.
#
# Interface changes versus the original reusable:
#   - `published` is now the package-set JSON array
#     ([{name, version, status, integrity}]) instead of a boolean.
#   - `version`, `package-name`, and `tarball` outputs are gone (read them
#     from the `published` JSON instead).
#   - The `npm-token` secret is accepted for signature compatibility but
#     IGNORED: publishing runs under npm trusted publishing (OIDC) only.

"on":
  workflow_call:
    inputs:
      node-version:
        description: >-
          Node.js version for publish. Trusted publishing needs npm >= 11.5.1;
          Node 24 ships a compatible npm. Do not self-upgrade npm in-place.
        required: false
        type: string
        default: "24"
      dist-tag:
        description: "npm dist-tag (latest, beta, next, etc.)"
        required: false
        type: string
        default: "latest"
      provenance:
        description: >-
          Pass --provenance on publish (explicit intent). Under OIDC trusted
          publishing provenance is also automatic.
        required: false
        type: boolean
        default: true
      access:
        description: "Package access level (public, restricted)"
        required: false
        type: string
        default: "public"
      dry-run:
        description: "Build and pack only, do not publish"
        required: false
        type: boolean
        default: false
      working-directory:
        description: "Directory containing the package to publish"
        required: false
        type: string
        default: "."
      tooling-ref:
        description: "Git ref to use when checking out lgtm-ci tooling scripts"
        required: false
        type: string
        default: ""
      egress-policy:
        description: "harden-runner egress policy (audit or block)"
        required: false
        type: string
        default: "block"
      allowed-endpoints:
        description: "Allowed endpoints when egress-policy is block"
        required: false
        type: string
        default: >
          github.com:443
          api.github.com:443
          actions.githubusercontent.com:443
          token.actions.githubusercontent.com:443
          codeload.github.com:443
          objects.githubusercontent.com:443
          raw.githubusercontent.com:443
          registry.npmjs.org:443
          fulcio.sigstore.dev:443
          rekor.sigstore.dev:443
          tuf-repo-cdn.sigstore.dev:443
          oauth2.sigstore.dev:443
      allowed-endpoints-mode:
        description: >-
          replace: non-empty allowed-endpoints overrides egress-preset; append:
          merges preset with allowed-endpoints (deduped, first-seen wins)
        required: false
        type: string
        default: "replace"
      egress-preset:
        description: >-
          Named allowlist when allowed-endpoints is empty and egress-policy is
          block (npm-publish)
        required: false
        type: string
        default: "npm-publish"
      job-name:
        description: "GitHub check name for the publish job"
        required: false
        type: string
        default: "Publish to npm (deprecated wrapper)"
      timeout-minutes:
        description: "Job timeout in minutes"
        required: false
        type: number
        default: 15

    secrets:
      npm-token:
        description: >-
          Ignored (accepted for migration compatibility): publishing runs
          under npm trusted publishing (OIDC) only.
        required: false

    outputs:
      published:
        description: >-
          JSON array of {name, version, status: skipped|published, integrity}
          (one entry)
        value: ${{ jobs.publish.outputs.published }}
      dist-tag-drift:
        description: Whether the dist-tag could not be reconciled
        value: ${{ jobs.publish.outputs.dist-tag-drift }}

permissions:
  contents: read

jobs:
  deprecation-notice:
    name: Deprecation notice
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Warn about the deprecated reusable
        shell: bash
        env:
          GITHUB_WORKFLOW_REF_VALUE: ${{ github.workflow_ref }}
        run: |
          echo "::warning::reusable-publish-npm.yml is deprecated and will be removed in a future release (#965)." \
            "Switch to reusable-publish-npm-set.yml (a single package is a set of one; see docs/reusable-workflows.md)."
          echo "Entry workflow: ${GITHUB_WORKFLOW_REF_VALUE:-unknown}"

  publish:
    name: ${{ inputs.job-name }}
    needs: [deprecation-notice]
    uses: ./.github/workflows/reusable-publish-npm-set.yml
    permissions:
      contents: read
      id-token: write
      attestations: write
    with:
      packages-dir: ${{ inputs.working-directory }}
      # Single-directory shape: the package IS the packages directory.
      order: "."
      dist-tag: ${{ inputs.dist-tag }}
      dry-run: ${{ inputs.dry-run }}
      provenance: ${{ inputs.provenance }}
      access: ${{ inputs.access }}
      node-version: ${{ inputs.node-version }}
      tooling-ref: ${{ inputs.tooling-ref }}
      egress-policy: ${{ inputs.egress-policy }}
      allowed-endpoints: ${{ inputs.allowed-endpoints }}
      allowed-endpoints-mode: ${{ inputs.allowed-endpoints-mode }}
      egress-preset: ${{ inputs.egress-preset }}
      job-name: ${{ inputs.job-name }}
      timeout-minutes: ${{ inputs.timeout-minutes }}
